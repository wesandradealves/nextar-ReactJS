import styled from 'styled-components';

export const SpinnerContainer = styled.div<{
  $overlay?: boolean;
}>`
  /* styled-components apenas para props dinâmicas que Tailwind não suporta diretamente */
`;
